"""HTML template fragment."""

PART = '\n    function renderEnergyTransferFlow(spec, scene, progress, sceneIndex = 0) {\n      const theme = spec.theme || {};\n      const semanticObjects = resolveSemanticObjects(spec, spec?.scenes || []);\n      const sceneCount = Array.isArray(spec.scenes) ? spec.scenes.length : 1;\n      const shot = resolveProcessShot(scene, sceneIndex, sceneCount);\n      const labels = semanticObjects.length > 0\n        ? semanticObjects.map((item) => item.label)\n        : ["能量源", "传递通道", "转换单元", "输出结果"];\n      const stageCount = labels.length;\n      const activeIndex = shot === "summary"\n        ? Math.max(stageCount - 1, 0)\n        : Math.min(stageCount - 1, Math.floor(progress * stageCount));\n      const flowX = 144 + clamp(progress, 0, 1) * 612;\n      const waveOffset = Math.sin(progress * Math.PI * 2) * 10;\n      const bullets = resolveBullets(scene, spec, 2);\n      return `\n        <g>\n          <rect x="42" y="154" width="876" height="344" rx="34" fill="${withAlpha(theme.panel, 0.96)}" stroke="${withAlpha(theme.accent, 0.18)}" />\n          <rect x="78" y="194" width="804" height="180" rx="28" fill="${withAlpha(theme.panel_alt, 0.78)}" stroke="${withAlpha(theme.accent, 0.20)}" />\n          <line x1="144" y1="286" x2="756" y2="286" stroke="${withAlpha(theme.accent, 0.34)}" stroke-width="12" stroke-linecap="round" />\n          <line x1="144" y1="286" x2="${flowX}" y2="286" stroke="${withAlpha(theme.highlight, 0.82)}" stroke-width="16" stroke-linecap="round" />\n          ${renderMotionTrail(144, 756, 286, progress, theme.highlight, 5)}\n          <circle cx="${flowX}" cy="${286 + waveOffset * 0.15}" r="18" fill="${theme.highlight}" stroke="${withAlpha(theme.accent_deep, 0.72)}" stroke-width="3" />\n          ${labels.map((label, index) => {\n            const x = 144 + index * (612 / Math.max(stageCount - 1, 1));\n            const active = index <= activeIndex;\n            const current = index === activeIndex;\n            return `\n              <g>\n                <circle cx="${x}" cy="286" r="${current ? 28 : 22}" fill="${active ? theme.accent : theme.panel}" stroke="${current ? withAlpha(theme.highlight, 0.88) : withAlpha(theme.accent, 0.28)}" stroke-width="${current ? 4 : 2.5}" />\n                <text x="${x}" y="293" text-anchor="middle" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="13" font-weight="800" fill="${active ? withAlpha(theme.panel, 0.96) : theme.accent_deep}">\n                  ${index + 1}\n                </text>\n                ${renderTextBlock(x - 56, 344, label, {\n                  fontSize: 14,\n                  fill: theme.text,\n                  lineHeight: 18,\n                  maxChars: 10,\n                  maxLines: 2,\n                  fontWeight: current ? 700 : 600,\n                })}\n              </g>\n            `;\n          }).join("")}\n          <rect x="94" y="394" width="338" height="72" rx="22" fill="${withAlpha(theme.highlight, 0.10)}" stroke="${withAlpha(theme.highlight, 0.26)}" />\n          <text x="122" y="422" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="20" font-weight="800" fill="${theme.accent_deep}">\n            当前能量段：${escapeHtml(labels[activeIndex] || "输出结果")}\n          </text>\n          ${renderTextBlock(122, 448, scene.description || spec.summary || "", {\n            fontSize: 14,\n            fill: theme.text,\n            lineHeight: 18,\n            maxChars: 24,\n            maxLines: 1,\n            fontWeight: 600,\n          })}\n          <rect x="464" y="394" width="390" height="72" rx="22" fill="${withAlpha(theme.panel, 0.94)}" stroke="${withAlpha(theme.accent, 0.18)}" />\n          ${bullets.map((point, index) => `\n            <g transform="translate(490, ${422 + index * 20})">\n              <circle cx="0" cy="0" r="4" fill="${theme.highlight}" />\n              ${renderTextBlock(12, 5, point, {\n                fontSize: 13,\n                fill: theme.text,\n                lineHeight: 16,\n                maxChars: 26,\n                maxLines: 1,\n                fontWeight: 600,\n              })}\n            </g>\n          `).join("")}\n        </g>\n      `;\n    }\n\n    function renderLifecycleFlow(spec, scene, progress, sceneIndex = 0) {\n      const theme = spec.theme || {};\n      const semanticObjects = resolveSemanticObjects(spec, spec?.scenes || []);\n      const sceneCount = Array.isArray(spec.scenes) ? spec.scenes.length : 1;\n      const shot = resolveProcessShot(scene, sceneIndex, sceneCount);\n      const labels = semanticObjects.length > 0\n        ? semanticObjects.map((item) => item.label)\n        : (spec.scenes || []).map((item) => item.title || "");\n      const visibleLabels = labels.slice(0, 5);\n      const activeIndex = shot === "summary"\n        ? Math.max(visibleLabels.length - 1, 0)\n        : Math.min(Math.max(visibleLabels.length - 1, 0), Math.floor(progress * Math.max(visibleLabels.length, 1)));\n      const centerX = 334;\n      const centerY = 322;\n      const radius = 118;\n      const orbit = visibleLabels.map((label, index) => {\n        const angle = (-Math.PI / 2) + (Math.PI * 2 * index) / Math.max(visibleLabels.length, 1);\n        return {\n          label,\n          x: centerX + Math.cos(angle) * radius,\n          y: centerY + Math.sin(angle) * radius,\n        };\n      });\n      const activeNode = orbit[activeIndex] || orbit[0] || { label: "当前阶段", x: centerX, y: centerY - radius };\n      const bullets = resolveBullets(scene, spec, 2);\n      return `\n        <g>\n          <rect x="40" y="154" width="880" height="344" rx="34" fill="${withAlpha(theme.panel, 0.96)}" stroke="${withAlpha(theme.accent, 0.18)}" />\n          <rect x="74" y="190" width="520" height="274" rx="30" fill="${withAlpha(theme.panel_alt, 0.78)}" stroke="${withAlpha(theme.accent, 0.18)}" />\n          <circle cx="${centerX}" cy="${centerY}" r="${radius}" fill="none" stroke="${withAlpha(theme.accent, 0.28)}" stroke-width="8" />\n          <circle cx="${centerX}" cy="${centerY}" r="${radius}" fill="none" stroke="${withAlpha(theme.highlight, 0.18)}" stroke-width="16" stroke-dasharray="${Math.round(radius * 2.1)} ${Math.round(radius * 4.1)}" stroke-dashoffset="${Math.round((1 - progress) * radius * 6)}" />\n          ${orbit.map((item, index) => {\n            const active = index === activeIndex;\n            return `\n              <g>\n                <circle cx="${item.x}" cy="${item.y}" r="${active ? 24 : 18}" fill="${active ? theme.highlight : theme.panel}" stroke="${active ? withAlpha(theme.accent_deep, 0.75) : withAlpha(theme.accent, 0.24)}" stroke-width="${active ? 3.5 : 2.2}" />\n                <text x="${item.x}" y="${item.y + 5}" text-anchor="middle" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="12" font-weight="800" fill="${active ? theme.text : theme.accent_deep}">\n                  ${index + 1}\n                </text>\n                ${renderTextBlock(item.x - 42, item.y + 38, item.label, {\n                  fontSize: 13,\n                  fill: theme.text,\n                  lineHeight: 16,\n                  maxChars: 8,\n                  maxLines: 2,\n                  fontWeight: active ? 700 : 600,\n                })}\n              </g>\n            `;\n          }).join("")}\n          <circle cx="${activeNode.x}" cy="${activeNode.y}" r="${32 + Math.sin(progress * Math.PI * 2) * 4}" fill="none" stroke="${withAlpha(theme.highlight, 0.24)}" stroke-width="3" />\n          <rect x="624" y="190" width="240" height="274" rx="26" fill="${withAlpha(theme.highlight, shot === "summary" ? 0.12 : 0.08)}" stroke="${withAlpha(theme.highlight, 0.24)}" />\n          <text x="652" y="226" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="22" font-weight="800" fill="${theme.text}">\n            ${shot === "summary" ? "阶段总览" : "当前阶段"}\n          </text>\n          ${renderTextBlock(652, 262, scene.description || activeNode.label || "", {\n            fontSize: 16,\n            fill: theme.text,\n            lineHeight: 22,\n            maxChars: 12,\n            maxLines: 4,\n            fontWeight: 600,\n          })}\n          ${renderChip(652, 354, `聚焦：${activeNode.label}`, withAlpha(theme.accent, 0.12), withAlpha(theme.accent, 0.24), theme.accent_deep)}\n          ${bullets.map((point, index) => `\n            <g transform="translate(654, ${404 + index * 22})">\n              <circle cx="0" cy="0" r="4" fill="${theme.highlight}" />\n              ${renderTextBlock(12, 5, point, {\n                fontSize: 13,\n                fill: theme.text,\n                lineHeight: 16,\n                maxChars: 14,\n                maxLines: 1,\n                fontWeight: 600,\n              })}\n            </g>\n          `).join("")}\n        </g>\n      `;\n    }\n\n    function renderProcessIntroScene(spec, scene, progress, sceneNodes) {\n      const theme = spec.theme || {};\n      const protocol = resolveProcessProtocol(spec, sceneNodes);\n      if (protocol.kind === "tcp_handshake" || protocol.kind === "tcp_teardown") {\n        const steps = protocol.steps;\n        const laneStartX = 236;\n        const laneEndX = 724;\n        const laneBaseY = 286;\n        const laneGap = 54;\n        const phase = clamp(progress * Math.max(steps.length, 1), 0, steps.length);\n        const lanes = steps.map((item, index) => {\n          const laneY = laneBaseY + index * laneGap;\n          const forward = item.from === "left";\n          const fromX = forward ? laneStartX : laneEndX;\n          const toX = forward ? laneEndX : laneStartX;\n          const localProgress = clamp(phase - index, 0, 1);\n          const eased = easeInOutCubic(localProgress);\n          const packetX = lerp(fromX, toX, eased);\n          const active = localProgress > 0.01;\n          const arrow = forward\n            ? "M 694 -8 L 724 0 L 694 8"\n            : "M 266 -8 L 236 0 L 266 8";\n          return `\n            <g opacity="${active ? 1 : 0.28}">\n              <line x1="${laneStartX}" y1="${laneY}" x2="${laneEndX}" y2="${laneY}" stroke="${withAlpha(theme.panel, 0.45)}" stroke-width="4" stroke-linecap="round" />\n              <path d="${arrow}" transform="translate(0, ${laneY})" fill="none" stroke="${withAlpha(theme.panel, 0.9)}" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round" />\n              <circle cx="${packetX}" cy="${laneY}" r="10" fill="${theme.highlight}" stroke="${withAlpha(theme.accent_deep, 0.72)}" stroke-width="2.5" />\n              ${renderTextBlock(forward ? 258 : 528, laneY - 12, item.packet, {\n                fontSize: 14,\n                fill: withAlpha(theme.panel, 0.95),\n                lineHeight: 18,\n                maxChars: 10,\n                maxLines: 1,\n                fontWeight: 700,\n              })}\n            </g>\n          `;\n        }).join("");\n        return `\n          <g>\n            <rect x="32" y="146" width="896" height="364" rx="36" fill="${withAlpha(theme.accent_deep, 0.9)}" stroke="${withAlpha(theme.panel, 0.25)}" />\n            <rect x="96" y="206" width="248" height="228" rx="24" fill="${withAlpha(theme.panel, 0.12)}" stroke="${withAlpha(theme.panel, 0.45)}" />\n            <rect x="616" y="206" width="248" height="228" rx="24" fill="${withAlpha(theme.panel, 0.12)}" stroke="${withAlpha(theme.panel, 0.45)}" />\n            <text x="220" y="286" text-anchor="middle" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="32" font-weight="800" fill="${withAlpha(theme.panel, 0.96)}">\n              客户端\n            </text>\n            <text x="740" y="286" text-anchor="middle" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="32" font-weight="800" fill="${withAlpha(theme.panel, 0.96)}">\n              服务器\n            </text>\n            <text x="96" y="186" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="28" font-weight="800" fill="${withAlpha(theme.panel, 0.96)}">\n              ${protocol.kind === "tcp_teardown" ? "TCP 四次挥手动态预览" : "TCP 三次握手动态预览"}\n            </text>\n            ${renderTextBlock(96, 454, scene.description || spec.summary || "", {\n              fontSize: 16,\n              fill: withAlpha(theme.panel, 0.9),\n              lineHeight: 22,\n              maxChars: 32,\n              maxLines: 1,\n              fontWeight: 600,\n            })}\n            ${lanes}\n          </g>\n        `;\n      }\n      const nodeChips = sceneNodes.slice(0, 4).map((item, index) =>\n        renderChip(\n          118 + index * 186,\n          404 + (index % 2 === 0 ? Math.sin((progress + index * 0.12) * Math.PI) * 8 : 0),\n          item.title || `步骤 ${index + 1}`,\n          withAlpha(theme.panel, 0.22),\n          withAlpha(theme.panel, 0.58),\n          withAlpha(theme.panel, 0.98)\n        )\n      ).join("");\n      const revealTrack = 124 + progress * 560;\n      return `\n        <g>\n          <rect x="32" y="146" width="896" height="364" rx="36" fill="${withAlpha(theme.accent_deep, 0.88)}" stroke="${withAlpha(theme.panel, 0.22)}" />\n          <circle cx="${198 + progress * 28}" cy="${262 + progress * 10}" r="84" fill="${withAlpha(theme.highlight, 0.24)}" />\n          <circle cx="${786 - progress * 24}" cy="${352 - progress * 8}" r="76" fill="${withAlpha(theme.accent, 0.28)}" />\n          <text x="92" y="224" font-family="\'Noto Sans CJK SC\', \'WenQuanYi Zen Hei\', \'Microsoft YaHei\', \'PingFang SC\', sans-serif" font-size="30" font-weight="800" fill="${withAlpha(theme.panel, 0.98)}">\n            镜头开场：先看整体流程\n          </text>\n          ${renderTextBlock(92, 268, scene.description || spec.summary || "", {\n            fontSize: 18,\n            fill: withAlpha(theme.panel, 0.92),\n            lineHeight: 24,\n            maxChars: 28,\n            maxLines: 2,\n            fontWeight: 600,\n          })}\n          <line x1="124" y1="384" x2="808" y2="384" stroke="${withAlpha(theme.panel, 0.28)}" stroke-width="4" stroke-linecap="round" />\n          <line x1="124" y1="384" x2="${revealTrack}" y2="384" stroke="${withAlpha(theme.highlight, 0.72)}" stroke-width="6" stroke-linecap="round" />\n          <circle cx="${revealTrack}" cy="384" r="10" fill="${theme.highlight}" stroke="${withAlpha(theme.panel, 0.85)}" stroke-width="2.5" />\n          ${nodeChips}\n        </g>\n      `;\n    }\n\n    function renderProcessFocusScene(spec, scene, progress, sceneIndex, sceneNodes) {\n      const theme = spec.theme || {};\n      const protocol = resolveProcessProtocol(spec, sceneNodes);\n      if (protocol.kind === "tcp_handshake" || protocol.kind === "tcp_teardown") {\n        const steps = protocol.steps;\n'
