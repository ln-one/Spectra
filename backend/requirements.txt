# Core Framework
fastapi==0.129.0
uvicorn[standard]==0.32.1
pydantic==2.12.5
pydantic-settings==2.13.1
prisma==0.15.0
python-multipart==0.0.22
aiofiles==25.1.0
python-dotenv==1.2.1

# Authentication & Security
python-jose[cryptography]==3.4.0  # JWT token handling (fixed CVE)
passlib[bcrypt]==1.7.4            # Password hashing
slowapi==0.1.9                    # Rate limiting

# AI & LLM (不使用 LangChain，仅用 LiteLLM 做路由)
litellm==1.81.13           # LLM 统一接口（支持 Qwen/GPT/Claude）
dashscope>=1.14.0          # 阿里云通义千问 SDK

# Vector Database & RAG
chromadb>=0.4.22           # 向量数据库
sentence-transformers>=2.2.2  # Embedding 模型

# Document Processing - 可插拔设计
llama-parse>=0.4.0         # LlamaParse 云端解析（备选，快速原型）
# magic-pdf               # MinerU 本地解析（主选，需单独安装）
# paddleocr               # 图片 OCR（需单独安装）

# Audio Processing (待集成)
# faster-whisper          # 语音识别（需单独安装）

# Courseware Generation (待集成)
# marp-cli 通过 npm 安装
# pandoc 通过系统包管理器安装

# Logging (optional, for JSON logging)
# python-json-logger==2.0.7


