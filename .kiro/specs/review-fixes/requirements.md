# 审核问题修复需求

## 目标

修复代码审核中发现的 P0 和 P1 问题，确保架构文档与代码一致。

## 背景

团队完成了架构审核，发现了 23 个问题：
- 8 个 P0 阻塞问题（必须修复）
- 12 个 P1 重要问题（建议修复）
- 3 个 P2 优化问题（可选）

由于当前只是脚手架代码，重点修复文档一致性和基础结构问题。

## 核心需求

### 1. 后端 P0 问题（4个）

#### #B1 认证链路
- 实现基础的 JWT 认证逻辑（不需要完整功能，骨架即可）
- 更新 `get_current_user` 依赖注入
- 文档与代码保持一致

#### #B2 数据隔离
- 在路由中添加用户隔离检查的注释和 TODO
- 更新安全文档说明

#### #B3 数据库参数
- 修复 Prisma 写入参数（添加必填字段）
- 确保代码可运行

#### #B4 API 契约
- 统一字段命名：Project 用 `name`，Auth 用 `access_token`
- 更新 OpenAPI 规范

### 2. 前端 P0 问题（4个）

#### #F1 API 方案
- 文档统一说明使用 Fetch API

#### #F2 文件拆分
- 拆分 `api.ts` 为多个模块（每个 <300 行）

#### #F3 Token 命名
- 统一使用 `access_token`

#### #F4 注册参数
- 统一注册参数命名

### 3. P1 问题（选择性修复）

- 统一文档中的 `/api/v1` 前缀
- 修复文档断链
- 统一错误响应格式文档

## 非需求

- 不需要实现完整的认证功能
- 不需要实现幂等性
- 不需要完整的表单校验
- P2 问题暂不处理

## 验证标准

- [ ] 所有 P0 问题已修复
- [ ] 文档与代码一致
- [ ] CI 通过
- [ ] 代码可运行（即使是骨架）
