/api/v1/generate/sessions:
  get:
    summary: 获取生成会话列表
    operationId: getGenerateSessions
    description: 返回项目内生成会话列表（按更新时间倒序）
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: project_id
        in: query
        required: true
        schema:
          type: string
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate.yaml#/GenerationSessionListResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
  post:
    summary: 创建生成会话
    operationId: postGenerateSessions
    description: 创建可中断、可恢复的课件生成会话（契约优先主入口）
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - $ref: '../components/parameters.yaml#/IdempotencyKey'
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate.yaml#/CreateGenerationSessionRequest'
    responses:
      '200':
        description: 会话创建成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate.yaml#/CreateGenerationSessionResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'

/api/v1/generate/sessions/{session_id}:
  get:
    summary: 查询生成会话快照
    operationId: getGenerateSessionsBySessionId
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate.yaml#/GenerationSessionResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'

/api/v1/generate/sessions/{session_id}/events:
  get:
    summary: 获取生成事件流（SSE）
    operationId: getGenerateSessionsBySessionIdEvents
    description: |
      默认返回 text/event-stream。当客户端无法使用 SSE 时，可通过 `accept=application/json`
      返回短轮询事件列表。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
      - name: cursor
        in: query
        required: false
        schema:
          type: string
        description: 事件游标（用于断线续传）
      - name: accept
        in: query
        required: false
        schema:
          type: string
          enum: [text/event-stream, application/json]
          default: text/event-stream
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          text/event-stream:
            schema:
              type: string
              description: SSE stream，data 字段符合 GenerationEvent
          application/json:
            schema:
              $ref: '../schemas/generate.yaml#/GenerationEventListResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'

/api/v1/generate/capabilities:
  get:
    summary: 获取生成域能力声明
    operationId: getGenerateCapabilities
    description: 返回服务端当前支持的契约版本、特性开关与弃用信息。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate.yaml#/GenerationCapabilitiesResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'

/api/v1/generate/studio-cards:
  get:
    summary: 获取 Studio 卡片协议目录
    operationId: getGenerateStudioCards
    description: 返回 Studio 卡片目录、成熟度、配置字段与上下文协议。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardsCatalogResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'

/api/v1/generate/studio-cards/{card_id}:
  get:
    summary: 获取单张 Studio 卡片协议
    operationId: getGenerateStudioCardById
    description: 返回单张 Studio 卡片的后端协议细节。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardDetailResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'

/api/v1/generate/studio-cards/{card_id}/execution-plan:
  get:
    summary: 获取单张 Studio 卡片执行协议
    operationId: getGenerateStudioCardExecutionPlan
    description: 返回单张 Studio 卡片当前可落地的后端执行绑定、缺口与结果字段。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionPlanResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'

/api/v1/generate/studio-cards/{card_id}/execution-preview:
  post:
    summary: 获取单张 Studio 卡片执行预览
    operationId: postGenerateStudioCardExecutionPreview
    description: 根据卡片配置返回当前可以直接调用的后端请求预览。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionPreviewRequest'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionPreviewResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'

/api/v1/generate/studio-cards/{card_id}/draft:
  post:
    summary: 为单张 Studio 卡片创建草稿
    operationId: postGenerateStudioCardDraft
    description: 为卡片创建 draft run，不触发最终生成结果落地。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionPreviewRequest'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
      '502':
        $ref: '../components/responses.yaml#/BadGateway'
      '503':
        $ref: '../components/responses.yaml#/ServiceUnavailable'
      '504':
        $ref: '../components/responses.yaml#/GatewayTimeout'

/api/v1/generate/studio-cards/{card_id}/execute:
  post:
    summary: 执行单张 Studio 卡片
    operationId: postGenerateStudioCardExecute
    description: 对已达到 foundation-ready 的卡片直接执行初始动作，返回实际落地的 session 或 artifact。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionPreviewRequest'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardExecutionResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
      '502':
        $ref: '../components/responses.yaml#/BadGateway'
      '503':
        $ref: '../components/responses.yaml#/ServiceUnavailable'
      '504':
        $ref: '../components/responses.yaml#/GatewayTimeout'

/api/v1/generate/studio-cards/{card_id}/refine:
  post:
    summary: 执行单张 Studio 卡片 refine
    operationId: postGenerateStudioCardRefine
    description: 通过统一 chat 通道执行已暴露 refine 协议的卡片局部改写。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate-capabilities.yaml#/StudioCardRefineRequest'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardRefineResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
      '502':
        $ref: '../components/responses.yaml#/BadGateway'
      '503':
        $ref: '../components/responses.yaml#/ServiceUnavailable'
      '504':
        $ref: '../components/responses.yaml#/GatewayTimeout'

/api/v1/generate/studio-cards/classroom_qa_simulator/turn:
  post:
    summary: 推进课堂问答模拟轮次
    operationId: postGenerateStudioCardSimulatorTurn
    description: 基于已有 classroom_qa_simulator artifact 追加或替换一轮问答模拟。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - $ref: '../components/parameters.yaml#/ContractVersion'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/generate-capabilities.yaml#/StudioCardTurnRequest'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardTurnResponse'
      '400':
        $ref: '../components/responses.yaml#/BadRequest'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '404':
        $ref: '../components/responses.yaml#/NotFound'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
      '502':
        $ref: '../components/responses.yaml#/BadGateway'
      '503':
        $ref: '../components/responses.yaml#/ServiceUnavailable'
      '504':
        $ref: '../components/responses.yaml#/GatewayTimeout'

/api/v1/generate/studio-cards/{card_id}/sources:
  get:
    summary: 获取 Studio 卡片可绑定源成果
    operationId: getGenerateStudioCardSources
    description: 返回当前卡片可选的源成果列表，优先用于 source-artifact 驱动的组合型卡片。
    tags: [Generate]
    security:
      - BearerAuth: []
    parameters:
      - name: card_id
        in: path
        required: true
        schema:
          type: string
      - name: project_id
        in: query
        required: true
        schema:
          type: string
      - $ref: '../components/parameters.yaml#/ContractVersion'
    responses:
      '200':
        description: 成功
        content:
          application/json:
            schema:
              $ref: '../schemas/generate-capabilities.yaml#/StudioCardSourceOptionsResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses.yaml#/Forbidden'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
