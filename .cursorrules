# Spectra Monorepo

> **AI 快速开始**: 请先阅读 `.ai/CONTEXT.md` 获取完整项目上下文

## AI 协作指南

本文件保持向后兼容。完整的 AI 协作规则已迁移到 `.ai/` 目录。

**推荐阅读顺序**：
1. `.ai/CONTEXT.md` - 项目概览和任务索引（必读）
2. `.ai/FAQ.md` - 常见问题快速解答
3. `.ai/guides/` - 任务驱动的操作指南
4. `.ai/self-check.md` - 验证你的理解

## AI 自主学习机制

**重要**: 当用户提出改进意见时，AI 必须：
1. 执行用户要求的修改
2. **主动分析**：用户纠正了什么问题？
3. **主动总结**：提炼出可复用的经验/规则
4. **主动更新**：将经验写入 `docs/standards/` 相关文档
5. 下次遇到类似情况时，参照更新后的规范执行

**示例**:
```
用户说: "文档太啰嗦，AI 不需要知道理念"
AI 应该:
1. 精简文档 ✅
2. 分析: 用户在纠正"文档分类不清"的问题
3. 总结: .cursorrules 给规则，docs/ 给理念
4. 更新: 将此原则写入 docs/standards/documentation.md
5. 下次写文档时，自动应用此原则
```

**禁止**: 等用户重复说同样的问题

## 权威规范

- `docs/openapi/` - API 契约（模块化，优先读取）
- `docs/standards/frontend.md` - 前端规范
- `docs/standards/backend.md` - 后端规范
- `docs/standards/git.md` - Git 规范
- `.ai/guides/` - 任务操作指南

## 核心规则

### Commit
```
<type>(<scope>): <subject>
```
Type: feat | fix | refactor | docs | style | test | chore

### 禁止
- 直接 Push 到 main
- 单文件 >300 行
- 单文档 >6000 字符
- 提交敏感信息

## API 开发

```bash
# 前端生成类型
npx openapi-typescript ../docs/openapi.yaml -o lib/types/api.ts

# 后端生成 Schema
datamodel-codegen --input ../docs/openapi.yaml --output schemas/generated.py
```

## 规则优先级

当规则冲突时，按以下顺序应用：

```
1. docs/openapi/ - API 契约（最高优先级）
2. docs/standards/ - 代码规范
3. .ai/guides/ - 任务指南
4. .cursorrules (根目录) - 全局规约
5. 子目录 .cursorrules - 领域特定规则
```

## 工具特定配置

当前仓库不再维护单独的 tool-specific 配置文件，所有 AI 工具（包括 Cursor、Kiro 等）统一遵循 `.ai/` 目录下的文档。

所有 `.ai/` 文档使用标准 Markdown 编写，无特定工具绑定，你可以在任意支持 Markdown 的环境中阅读和遵循这些规范。
