SendMessageResponseTarget:
  allOf:
    - $ref: './chat.yaml#/SendMessageResponse'
    - type: object
      properties:
        data:
          type: object
          properties:
            session_title_updated:
              type: boolean
            session_title:
              type: string
            session_title_source:
              type: string
              enum: [default, first_message, manual]
