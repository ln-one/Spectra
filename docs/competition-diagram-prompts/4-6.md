# 4-6 关键接口与契约总览图

## 版本

`单版本`

## 默认适配场景

`PPT / Word 通用`

## 图类型

`契约 / 泳道实现图`

## 这张图只回答什么

`Spectra` 后端为什么更像 contract surface，而不是正式能力本体。

## 主阅读路径

从左到右看请求进入、控制平面编排、外部 authority 返回、结果绑定。

## 来源与事实锚点

- `docs/competition/04-architecture.md`
- `docs/architecture/api-contract.md`
- `docs/architecture/backend/overview.md`

## 现有图问题检测

- 容易写成参数表
- 容易把 backend 画成能力 owner
- `结论`：`需中度重构`

## 信息分层设计

- 入口层
- 控制平面层
- 外部 authority 层
- 回流绑定层

## 分组设计

- 左：请求与合同入口
- 中：Spectra 控制平面
- 右：外部 authority
- 底：结果绑定与下载

## 密度策略

- `中密度`
- 保留关键 contract 类型，不展开参数细节

## 画幅与布局约束

- 横向优先，兼容正文
- 卡片式四区布局
- 箭头数量控制在主关系级别

## 优化后的 Mermaid 骨架

```mermaid
flowchart LR
    A["请求入口"]
    B["Contract Surface"]
    C["Session / Run 编排"]
    D["Diego / Pagevra / Ourograph"]
    E["Artifact Binding"]
    F["下载与回流"]

    A --> B --> C --> D --> E --> F
```

## 中文手绘主 Prompt

请重绘一张用于中国高校竞赛答辩或正文的关键接口与契约总览图。  
这张图强调 `Spectra` 后端是 `contract surface` 和 `orchestration kernel`，不是正式能力本体。  
画面采用横向四到六段结构：`请求入口`、`Contract Surface`、`Session / Run编排`、`Diego / Pagevra / Ourograph`、`Artifact Binding`、`下载与回流`。  
重点是接口类型与结果绑定，不是参数表。  
整体风格专业、高级、低饱和、克制、简约多彩，中文标签大而短，适合 PPT 和 Word 通用使用。

## 英文补充关键词（可选）

- `contract surface diagram`
- `service facade map`
- `clear horizontal lanes`
- `low saturation`
- `readable labels`

## 统一风格负面约束

- 禁止写成 API 参数表
- 禁止把 backend 画成三个能力的拥有者
- 禁止展开过多细节箭头
- 禁止默认流程图小字风格

## 审图备注

- 这张图要更像“契约总览”而不是“接口列表”。
- `Artifact Binding` 需要明显，但不能抢走主中轴。
