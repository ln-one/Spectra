services:
  ourograph:
    image: spectra-ourograph:dev
    build:
      context: .
      dockerfile: ourograph/Dockerfile.dev
    volumes:
      - ./ourograph:/app
      - runtime_data:/var/lib/spectra
