# Docs

> **AI 快速开始**: 请先阅读 `../.ai/CONTEXT.md` 获取完整项目上下文

## 文档任务指南

**必读文档**：
- `../.ai/CONTEXT.md` - 项目概览（必读）
- `../.ai/guides/best-practices.md` - 最佳实践
- `standards/documentation.md` - 文档规范

## 重要

本目录包含项目权威规范：
- `openapi/` - API 契约（模块化文件）
- `openapi.yaml` - API 契约（打包后的完整文件，自动生成）
- `standards/` - 代码规范
- `architecture/` - 架构设计
- `decisions/` - 技术决策

## 规则

### 格式
- Markdown (.md)
- 单文档 <6000 字符
- Mermaid 绘图
- 精简，少废话

### 目录
```
openapi/         # API 契约（模块化）
project/         # 项目基础
standards/       # 规范 (权威)
architecture/    # 架构 (权威)
decisions/       # 决策 (ADR)
requirements/    # 需求
guides/          # 指南
```

## Mermaid

```mermaid
graph LR
    A[用户] --> B[前端]
    B --> C[后端]
```

## 维护

- 代码变更时更新文档
- 废弃决策标记 "已废弃"
- 修改 `openapi/` 后运行 `npm run bundle:openapi` 打包
- `openapi.yaml` 与实现一致（自动生成，不要手动编辑）

## Commit

```
docs(<scope>): <subject>
```
