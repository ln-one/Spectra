# 成员 A 交付物（A3）产品级上线门禁看板

> 日期：2026-03-06
> 使用方式：每个模块 PR 必须关联至少一条 DoD 项，并更新勾选状态。

## 产品级 DoD（可勾选）

## D1 可用性（Usability）
- [ ] D1-1 用户可完整走通：配置 -> 大纲共创 -> 确认生成 -> 预览编辑 -> 局部重绘 -> 导出。
- [ ] D1-2 关键交互均调用真实 API，无关键路径 Mock 依赖。
- [ ] D1-3 前端可稳定显示成功/失败/降级状态，不依赖隐式判断。

## D2 稳定性（Stability）
- [ ] D2-1 任务状态可追踪（提交、执行、完成、失败）并具备错误码。
- [ ] D2-2 异常路径可重复复现（至少覆盖超时、依赖不可用、输入非法）。
- [ ] D2-3 关键路径具备回归测试（API/E2E）。

## D3 可恢复（Recoverability）
- [ ] D3-1 失败后可重试/恢复，不需用户重建项目。
- [ ] D3-2 幂等键场景可防重复写入与重复执行。
- [ ] D3-3 中断后可继续任务并保持状态一致。

## D4 可溯源（Traceability）
- [ ] D4-1 输出页存在来源入口。
- [ ] D4-2 来源字段至少具备 `chunk/file/page/time` 中可解释的最小集合。
- [ ] D4-3 失败与降级可关联 `trace_id` 或同等级链路标识。

## D5 能力补齐与降级（Capability + Degradation）
- [ ] D5-1 MinerU（主）+ LlamaParse（备）可实际调用且可切换。
- [ ] D5-2 Qwen-VL 视频理解链路可实际调用并返回结构化结果。
- [ ] D5-3 Faster-Whisper 语音识别链路可实际调用并返回结构化结果。
- [ ] D5-4 外部能力异常时自动降级且主流程不断。
- [ ] D5-5 降级返回字段完整：`fallback_used/fallback_target/reason_code/user_message`。

## 上线准入结论（2026-03-06）

- 当前结论：**不满足上线准入**。
- 统计：0/17 全通过（关键阻塞集中在 D1-1、D5-1、D5-2、D5-3）。

## 当前阻塞清单（按严重度）

1. P0：三能力真实接入未闭环（MinerU/LlamaParse、Qwen-VL、Faster-Whisper）。
2. P0：完整用户旅程尚未在统一会话状态机下闭环验证。
3. P1：统一降级语义虽已在契约定义，但真实链路返回样例不足。

## PR -> DoD 映射规则（执行规范）

每个 PR 描述必须包含：

- `DoD-MAP:` 对应条目（如 `D5-3, D2-1`）
- `验证证据:` 测试名/脚本/截图/日志链接
- `风险与回滚:` 失败时如何回退

不满足以上 3 项的 PR，不进入合并。

